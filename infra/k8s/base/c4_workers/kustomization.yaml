# =============================================================================
# File: kustomization.yaml
# Version: 1
# Path: infra/k8s/base/c4_workers/kustomization.yaml
# Description: Kustomization bundle for the C4 sub-agent dispatch
#              infrastructure (R-200-030..033). Listed under the
#              top-level `infra/k8s/base/kustomization.yaml` so a
#              single `kubectl apply -k infra/k8s/base` provisions
#              the c4-workers namespace, SAs, RBAC, NetworkPolicy.
# =============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - rbac.yaml
  - networkpolicy.yaml
