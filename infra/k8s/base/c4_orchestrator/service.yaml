# =============================================================================
# File: service.yaml
# Version: 1
# Path: infra/k8s/base/c4_orchestrator/service.yaml
# Description: ClusterIP Service for C4 Orchestrator.
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: c4-orchestrator
  namespace: aywizz
  labels:
    app.kubernetes.io/name: c4-orchestrator
    app.kubernetes.io/component: c4
    app.kubernetes.io/part-of: aywizz-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: c4-orchestrator
    app.kubernetes.io/component: c4
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
