# =============================================================================
# File: service.yaml
# Version: 1
# Path: infra/k8s/base/c9_mcp/service.yaml
# Description: ClusterIP Service for C9 MCP.
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: c9-mcp
  namespace: aywizz
  labels:
    app.kubernetes.io/name: c9-mcp
    app.kubernetes.io/component: c9
    app.kubernetes.io/part-of: aywizz-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: c9-mcp
    app.kubernetes.io/component: c9
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
